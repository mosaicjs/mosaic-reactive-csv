{
  "name": "mosaic-reactive-csv",
  "version": "0.0.2",
  "description": "CSV read/write library",
  "main": "index.js",
  "scripts": {
    "test": "mocha -R spec ./test/index.js",
    "bump": "npm test && npm version patch && git push && git push --tags && npm publish",
    "bump-minor": "npm run bump && npm version minor && git push && git push --tags && npm publish",
    "commit": "npm test && git commit -a"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mosaicjs/mosaic-reactive-csv.git"
  },
  "keywords": [],
  "author": "Mikhail Kotelnikov <mikhail.kotelnikov@ubimix.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/mosaicjs/mosaic-reactive-csv/issues"
  },
  "devDependencies": {
    "expect.js": "^0.3.0",
    "mocha": "^2.4.0"
  },
  "dependencies": {
    "d3-dsv": "^1.0.0",
    "mosaic-reactive": "^0.1.0"
  },
  "homepage": "https://github.com/mosaicjs/mosaic-reactive-csv#readme",
  "directories": {
    "test": "test"
  }
}
